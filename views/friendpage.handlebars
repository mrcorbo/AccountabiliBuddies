<!-- Button trigger modal -->

{{!-- <div>
  <button type="button" class="btn btn-dark" data-toggle="modal" data-target="#exampleModalCenter">
    Add newfriend
  </button>
</div>

<!-- Modal -->
<div class="modal fade" id="exampleModalCenter" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle"
  aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLongTitle">Add new buddy</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <!-- Form to include the other user's email -->
        <form id="friendEmailForm">
          <div class="form-group">
            <label for="exampleInputEmail1">Buddy's email</label>
            <input class="form-control" id="emailfriend" name="titleNewPost" aria-describedby="emailHelp"
              placeholder="Enter user's email">
          </div>
          <button type="submit" class="btn btn-primary" id="createBtn">Add</button>
        </form>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button type="button" class="btn btn-dark">Message</button>
      </div>
    </div>
  </div>
</div>

<div>
  Buddies:
  <p id="userfriend"></p>
</div> --}}


<form class="mx-1 mx-md-4" id="friendEmailForm">

  <div class="d-flex flex-row align-items-center mb-4">
    <i class="fas fa-user fa-lg me-3 fa-fw"></i>
    <div class="form-outline flex-fill mb-0">
      <input type="text" id="emailFriend" class="form-control" />
      <label class="form-label" for="form3Example1c">Buddie's Email</label>
    </div>
  </div>

  <div class="d-flex justify-content-center mx-4 mb-3 mb-lg-4">
    <button class="btn btn-outline-info me-2" type="submit">Add</button>
  </div>

</form>

{{!-- <div>
  Buddies:

  <div class="card border-light mb-3" style="max-width: 18rem;">
    <div class="card-header" id="friendEmail"></div>
    <div class="card-body">
      <h5 class="card-title">Accountabilities</h5>
      <p class="card-text"></p>
    </div>
  </div>

</div> --}}

<h5> Buddies </h5>
{{#if friends.length}}
<div id = "friendCard">
{{!-- <div id="userfriend"> --}}
  {{#each friends as |friend| }}
  <div class="goal-card card m-3" style="width: 18rem;">
    <div class="card-body">
      <!--<h5 class="card-title"><a href="/user/{{id}}">{{user.email}}</a></h5>-->
      {{!-- <h5 class="card-title"><a href="/friend/{{friend.id}}">{{friend.user.email}}</a></h5> --}}
      {{> friend}}
      {{!-- !-- <p class="card-text">{{friend.goal.name}} = {{friend.goal.frequency}} times a week for
        {{friend.goal.duration}} weeks</p> -- --}}
    </div>
    <button class="btn btn-danger pull-right" data-id="{{friend.id}}">Delete</button>
  </div>
  {{/each}}
  </div>
  {{/if}}

  <h5> Goals </h5>
{{#if goals.length}}
<div id = "friendCard">
{{!-- <div id="userfriend"> --}}
  {{#each goals as |goal| }}
  <div class="goal-card card m-3" style="width: 18rem;">
    <div class="card-body">
      <!--<h5 class="card-title"><a href="/user/{{id}}">{{user.email}}</a></h5>-->
      {{!-- <h5 class="card-title"><a href="/friend/{{friend.id}}">{{friend.user.email}}</a></h5> --}}
      <li>{{goal.name}}</li>
      <li>{{goal.duration}}</li>
      <li>{{goal.frequency}}</li>
      {{!-- !-- <p class="card-text">{{friend.goal.name}} = {{friend.goal.frequency}} times a week for
        {{friend.goal.duration}} weeks</p> -- --}}
    </div>
    <button class="btn btn-danger pull-right" data-id="{{goal.id}}">Delete</button>
  </div>
  {{/each}}
  </div>
  {{/if}}


<ul id = "friendGoals">

</ul>


 <script src="./js/friend.js"></script>
